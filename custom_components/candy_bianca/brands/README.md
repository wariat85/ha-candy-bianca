# Candy Bianca brand assets

Brand images are bundled with the integration (icon.svg, logo.svg) so HACS installs them alongside the code. This lets Home Assistant load the Candy Bianca icon in the Integrations page even when the separate `brands/` folder is not copied during installation.
